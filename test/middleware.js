var gitserve = require('../');

describe('Connect/Express Middleware', function () {

  before(function () {
    //  Here we need to 
  });

  describe('when cloning a remote repository', function () {

    it('should respond with a ')

  });

});