{
  "name": "gitserve",
  "description": "Middleware for git fetch, clone, push and pull",
  "version": "0.2.0",
  "main": "index",
  "devDependencies": {
    "mocha": "~1.13.0",
    "supertest": "~0.8.0",
    "should": "~2.0.2"
  }
}
